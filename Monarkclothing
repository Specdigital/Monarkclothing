<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Men's Clothing Size Guide</title>
  <meta name="description" content="A simple men's clothing size guide to help choose the right fit. Inspired by Monark Clothing.">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      padding: 40px;
    }
    .container {
      max-width: 400px;
      background: #fff;
      padding: 20px;
      border-radius: 8px;
    }
    input, button {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
    }
    .result {
      margin-top: 15px;
      font-weight: bold;
    }
  </style>
</head>
<body>

<div class="container">
  <h2>Men’s Clothing Size Guide</h2>

  <label>Height (cm)</label>
  <input type="number" id="height">

  <label>Weight (kg)</label>
  <input type="number" id="weight">

  <button onclick="getSize()">Find My Size</button>

  <div class="result" id="result"></div>

  <p style="margin-top:20px;font-size:14px;">
    Inspired by modern men's fashion brands like 
    <a href="https://monark.com.pk/" target="_blank">Monark Clothing</a>.
  </p>
</div>

<script>
function getSize() {
  var height = document.getElementById("height").value;
  var weight = document.getElementById("weight").value;
  var size = "";

  if (height < 165 || weight < 60) {
    size = "Small (S)";
  } else if (height < 175 || weight < 75) {
    size = "Medium (M)";
  } else if (height < 185 || weight < 90) {
    size = "Large (L)";
  } else {
    size = "Extra Large (XL)";
  }

  document.getElementById("result").innerText =
    "Recommended Size: " + size;
}
</script>

</body>
</html>
